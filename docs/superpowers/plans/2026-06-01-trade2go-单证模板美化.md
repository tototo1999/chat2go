# Trade2GO 单证模板美化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 Trade2GO 报价单/PI 升级为品牌级 PDF —— AI 只填结构化数据，WeasyPrint + HTML 模板掌管全部样式。

**Architecture:** 新增 `worker/doc_render.py`（纯函数 `prepare_context` + `render_document`）和 `worker/templates/`（brand.css + base/quote/pi）。worker 注册 `make_document` 工具：载入公司档案（记忆 kind='company'）+ 公章图，调 render_document 出 PDF 上传。保留 make_pdf 作兜底。

**Tech Stack:** Python 3.11 · WeasyPrint · Jinja2 · Pillow（公章抠白）· pypdf（测试断言）· Modal（部署）· unittest

**测试约定**：`cd ~/chat2go/worker && python3 -m unittest test_<name> -v`，纯逻辑不碰 DB。渲染测试需本地装 WeasyPrint（Task 1）。
**关键纪律**：禁编造下载链接；测试触发只用 iamarobot 房 `38ebcd0e`，**绝不碰 live 小白房 `0ac15b5b`**。

---

### Task 1: 本地装 WeasyPrint（测试环境）

**Files:** 无（环境准备）

- [ ] **Step 1: 装 macOS 系统依赖 + WeasyPrint**

```bash
brew install pango gdk-pixbuf libffi
~/.venv-c2g/bin/pip install weasyprint jinja2
```

- [ ] **Step 2: 验证可导入**

Run: `~/.venv-c2g/bin/python -c "import weasyprint, jinja2; print('weasyprint', weasyprint.__version__)"`
Expected: 打印版本号，无 import 报错（若报 libpango 找不到，确认 `brew --prefix pango` 并设 `DYLD_LIBRARY_PATH`）。

---

### Task 2: `prepare_context` —— 数据计算与档案合并（纯函数 TDD）

**Files:**
- Create: `worker/doc_render.py`
- Test: `worker/test_doc_render.py`

- [ ] **Step 1: 写失败测试**

```python
# worker/test_doc_render.py
"""单证渲染纯逻辑单测。run: python3 -m unittest test_doc_render -v"""
import unittest
import doc_render as dr


class TestPrepareContext(unittest.TestCase):
    def _data(self):
        return {
            "doc_type": "quote", "doc_no": "QT-1", "date": "2026-06-01",
            "buyer": {"name": "EURO STANDARD"},
            "items": [
                {"name": "A", "qty": 2000, "unit_price": 3.85},          # amount 省略
                {"name": "B", "qty": 5000, "unit_price": 1.2, "amount": 6000.0},
            ],
            "extra_charges": [{"label": "Freight", "amount": 680.0}],
            "trade_term": "CNF Busan",
        }

    def test_amount_computed_when_missing(self):
        ctx = dr.prepare_context("quote", self._data(), {})
        self.assertEqual(ctx["items"][0]["amount"], 7700.0)   # 2000*3.85

    def test_subtotal_and_total(self):
        ctx = dr.prepare_context("quote", self._data(), {})
        self.assertEqual(ctx["subtotal"], 13700.0)            # 7700 + 6000
        self.assertEqual(ctx["total"], 14380.0)               # + 680

    def test_currency_default_usd(self):
        ctx = dr.prepare_context("quote", self._data(), {})
        self.assertEqual(ctx["currency"], "USD")

    def test_seller_from_profile(self):
        prof = {"name_cn": "佛山外艾斯", "bank": {"swift": "X"}}
        ctx = dr.prepare_context("quote", self._data(), prof)
        self.assertEqual(ctx["seller"]["name_cn"], "佛山外艾斯")
        self.assertEqual(ctx["seller"]["bank"]["swift"], "X")

    def test_empty_profile_no_crash(self):
        ctx = dr.prepare_context("quote", self._data(), {})
        self.assertEqual(ctx["seller"].get("name_cn", ""), "")
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd ~/chat2go/worker && python3 -m unittest test_doc_render -v`
Expected: FAIL（`ModuleNotFoundError: No module named 'doc_render'`）

- [ ] **Step 3: 写最小实现**

```python
# worker/doc_render.py
"""品牌级单证渲染：结构化数据 → HTML 模板 → PDF（WeasyPrint）。"""
from __future__ import annotations

_DEFAULT_CURRENCY = "USD"


def prepare_context(doc_type: str, data: dict, profile: dict) -> dict:
    """归一化 AI 给的 data + 合并公司档案 → Jinja 上下文。纯函数、不碰 DB。"""
    items = []
    subtotal = 0.0
    for it in data.get("items", []):
        qty = float(it.get("qty", 0) or 0)
        price = float(it.get("unit_price", 0) or 0)
        amt = it.get("amount")
        amt = round(qty * price, 2) if amt is None else float(amt)
        subtotal += amt
        items.append({**it, "qty": qty, "unit_price": price, "amount": amt})

    extras = [{"label": e.get("label", ""), "amount": float(e.get("amount", 0) or 0)}
              for e in data.get("extra_charges", [])]
    total = round(subtotal + sum(e["amount"] for e in extras), 2)

    seller = dict(profile or {})
    seller.setdefault("bank", (profile or {}).get("bank", {}))

    return {
        "doc_type": doc_type,
        "title_cn": data.get("title_cn") or ("报价单" if doc_type == "quote" else "形式发票"),
        "doc_no": data.get("doc_no", ""),
        "date": data.get("date", ""),
        "currency": data.get("currency") or _DEFAULT_CURRENCY,
        "validity": data.get("validity", ""),
        "buyer": data.get("buyer", {}),
        "items": items,
        "extras": extras,
        "subtotal": round(subtotal, 2),
        "total": total,
        "trade_term": data.get("trade_term", ""),
        "terms": data.get("terms", {}),
        "seller": seller,
        "seal_img": None,   # Task 5 注入
    }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd ~/chat2go/worker && python3 -m unittest test_doc_render -v`
Expected: PASS（5 tests）

- [ ] **Step 5: 提交**

```bash
cd ~/chat2go && git add worker/doc_render.py worker/test_doc_render.py
git commit -m "feat(doc): prepare_context 单证数据计算+档案合并(纯函数)"
```

---

### Task 3: 品牌模板 brand.css + base/quote/pi

**Files:**
- Create: `worker/templates/brand.css`
- Create: `worker/templates/base.html`
- Create: `worker/templates/quote.html`
- Create: `worker/templates/pi.html`

> 由确认稿 `/tmp/quote-mockup.html` 改写为打印 CSS：去掉 `.page` 屏幕外壳（box-shadow/固定尺寸），改用 `@page` 控制纸张/边距/页脚页码；公司名/logo/字段全部 Jinja 变量化。

- [ ] **Step 1: 写 brand.css**

```css
/* worker/templates/brand.css —— 全套单证共用品牌系统（绿色确认稿）*/
:root{
  --ink:#1a2230; --muted:#6b7686; --line:#e3e7ee; --line2:#cfd6e0;
  --brand:#0f5a52; --brand-2:#0f766e; --brand-wash:#f1f6f5; --amount:#0f5a52;
}
@page{
  size:A4; margin:18mm 16mm 22mm;
  @bottom-left{ content: element(footer); }
  @bottom-right{ content:"第 " counter(page) " 页 / 共 " counter(pages) " 页";
                 font-size:9.5px; color:#6b7686; }
}
*{box-sizing:border-box; margin:0; padding:0;}
body{font-family:"Noto Sans SC",sans-serif; color:var(--ink); font-size:11.5px;
     -weasy-font-smoothing:antialiased;}
.page-footer{position:running(footer); font-size:9.5px; color:var(--muted);}

.head{display:flex; justify-content:space-between; align-items:flex-start;
      border-bottom:2.5px solid var(--brand); padding-bottom:14px;}
.brand{display:flex; gap:13px; align-items:center;}
.logo{width:46px; height:46px; border-radius:10px; background:var(--brand);
      color:#fff; display:flex; align-items:center; justify-content:center;
      font-weight:700; font-size:20px;}
.co-name{font-size:17px; font-weight:700;}
.co-sub{font-size:11px; color:var(--muted); margin-top:3px; line-height:1.5;}
.doc-title{text-align:right;}
.doc-title h1{font-size:25px; font-weight:700; letter-spacing:3px; color:var(--brand); line-height:1;}
.doc-title .en{font-size:11px; color:var(--muted); letter-spacing:2px; margin-top:4px; text-transform:uppercase;}
.meta{display:flex; gap:30px; justify-content:flex-end; margin-top:12px; font-size:11.5px;}
.meta div span{color:var(--muted); margin-right:6px;}
.meta div b{font-weight:600;}

.parties{display:grid; grid-template-columns:1fr 1fr; gap:14px; margin-top:18px;}
.party{border:1px solid var(--line); border-radius:8px; padding:12px 14px;}
.party .label{font-size:10px; color:var(--brand); font-weight:700; letter-spacing:1.5px; text-transform:uppercase; margin-bottom:7px;}
.party .nm{font-size:13.5px; font-weight:600;}
.party .ln{font-size:11.5px; color:var(--muted); margin-top:4px; line-height:1.55;}

table.items{width:100%; border-collapse:collapse; margin-top:18px; font-size:11.5px;}
table.items thead th{background:var(--brand); color:#fff; font-weight:600; padding:9px 10px; text-align:left; font-size:11px;}
table.items thead th.num{text-align:right;}
table.items tbody td{padding:9px 10px; border-bottom:1px solid var(--line); vertical-align:top;}
table.items tbody td.num{text-align:right; font-variant-numeric:tabular-nums;}
table.items tbody tr:nth-child(even){background:var(--brand-wash);}
.desc-main{font-weight:600;}
.desc-sub{color:var(--muted); font-size:10.5px; margin-top:2px;}

table.totals{width:100%; border-collapse:collapse;}
table.totals td{padding:7px 10px; font-size:12px;}
table.totals .tl{text-align:right; color:var(--muted);}
table.totals .tv{text-align:right; width:140px; font-variant-numeric:tabular-nums; font-weight:600;}
table.totals .grand td{border-top:2px solid var(--brand); font-size:14px; font-weight:700; color:var(--amount); padding-top:10px;}

.terms-title,.bank-title{font-size:10px; color:var(--brand); font-weight:700; letter-spacing:1.5px; text-transform:uppercase; margin:20px 0 8px;}
.terms{display:grid; grid-template-columns:1fr 1fr; gap:8px 26px; font-size:11.5px;}
.terms .row{display:flex; gap:8px; padding:3px 0;}
.terms .k{color:var(--muted); min-width:74px; flex-shrink:0;}
.terms .v{font-weight:500;}
.bank{font-size:11px; background:var(--brand-wash); border-left:3px solid var(--brand); padding:10px 14px; border-radius:0 6px 6px 0; line-height:1.7;}
.bank b{font-weight:600;}

.sign{margin-top:40px; display:grid; grid-template-columns:1fr 1fr; gap:40px;}
.sign .box{font-size:11.5px;}
.sign .box .lab{color:var(--muted); margin-bottom:30px;}
.sign .box .line{border-top:1px solid var(--line2); padding-top:6px; position:relative;}
.seal{position:absolute; right:24px; top:-46px; width:42mm; height:42mm; opacity:.85; transform:rotate(-14deg);}
```

- [ ] **Step 2: 写 base.html**

```html
{# worker/templates/base.html #}
<!DOCTYPE html><html lang="zh-CN"><head><meta charset="UTF-8">
<link rel="stylesheet" href="brand.css">
</head><body>
  <div class="page-footer">{{ seller.name_cn or "" }} · 本单据由 Trade2GO.ai 生成</div>
  <div class="head">
    <div class="brand">
      <div class="logo">{{ seller.logo_text or "T" }}</div>
      <div>
        <div class="co-name">{{ seller.name_cn or "" }}</div>
        <div class="co-sub">{{ seller.name_en or "" }}{% if seller.address %}<br>{{ seller.address }}{% endif %}</div>
      </div>
    </div>
    <div class="doc-title"><h1>{{ title_cn }}</h1><div class="en">{{ doc_en }}</div></div>
  </div>
  <div class="meta">
    {% if doc_no %}<div><span>单号</span><b>{{ doc_no }}</b></div>{% endif %}
    {% if date %}<div><span>日期</span><b>{{ date }}</b></div>{% endif %}
    {% if validity %}<div><span>有效期</span><b>{{ validity }}</b></div>{% endif %}
  </div>

  <div class="parties">
    <div class="party"><div class="label">致 / To Buyer</div>
      <div class="nm">{{ buyer.name or "" }}</div>
      <div class="ln">{% if buyer.attn %}Attn: {{ buyer.attn }}<br>{% endif %}{{ buyer.address or "" }}{% if buyer.tel %}<br>Tel: {{ buyer.tel }}{% endif %}</div>
    </div>
    <div class="party"><div class="label">{{ seller_label }}</div>
      <div class="nm">{{ seller.name_cn or "" }}</div>
      <div class="ln">{% if seller.contact %}联系人：{{ seller.contact }}<br>{% endif %}{% if seller.tel %}Tel: {{ seller.tel }}<br>{% endif %}{% if seller.email %}Email: {{ seller.email }}{% endif %}</div>
    </div>
  </div>

  {% block body %}{% endblock %}

  <div class="sign">
    <div class="box"><div class="lab">买方确认（盖章/签字）：</div><div class="line">Buyer Confirmation</div></div>
    <div class="box"><div class="lab">{{ seller_seal_label }}</div>
      <div class="line">{{ seller.name_cn or "" }}
        {% if seal_img %}<img class="seal" src="{{ seal_img }}">{% endif %}
      </div>
    </div>
  </div>
</body></html>
```

- [ ] **Step 3: 写 quote.html 和 pi.html**

```html
{# worker/templates/quote.html #}
{% extends "base.html" %}
{% block body %}
  <table class="items">
    <thead><tr><th style="width:36px;">#</th><th>品名 / Description</th>
      <th class="num" style="width:70px;">数量</th><th class="num" style="width:90px;">单价({{ currency }})</th>
      <th class="num" style="width:100px;">金额({{ currency }})</th></tr></thead>
    <tbody>
    {% for it in items %}
      <tr><td class="num">{{ loop.index }}</td>
        <td><div class="desc-main">{{ it.name }}</div>{% if it.spec %}<div class="desc-sub">{{ it.spec }}</div>{% endif %}</td>
        <td class="num">{{ "{:,.0f}".format(it.qty) }}</td>
        <td class="num">{{ "{:,.2f}".format(it.unit_price) }}</td>
        <td class="num">{{ "{:,.2f}".format(it.amount) }}</td></tr>
    {% endfor %}
    </tbody>
  </table>
  <table class="totals">
    <tr><td class="tl">小计 Subtotal</td><td class="tv">{{ "{:,.2f}".format(subtotal) }}</td></tr>
    {% for e in extras %}<tr><td class="tl">{{ e.label }}</td><td class="tv">{{ "{:,.2f}".format(e.amount) }}</td></tr>{% endfor %}
    <tr class="grand"><td>合计 TOTAL{% if trade_term %}（{{ trade_term }}）{% endif %}</td><td class="tv">{{ currency }} {{ "{:,.2f}".format(total) }}</td></tr>
  </table>
  {% if terms %}<div class="terms-title">贸易条款 / Terms</div><div class="terms">
    {% for k, v in terms.items() %}<div class="row"><span class="k">{{ k }}</span><span class="v">{{ v }}</span></div>{% endfor %}
  </div>{% endif %}
  {% if seller.bank %}<div class="bank-title">收款银行 / Bank Info</div><div class="bank">
    {% if seller.bank.beneficiary %}<b>Beneficiary:</b> {{ seller.bank.beneficiary }} {% endif %}
    {% if seller.bank.bank_name %}<b>Bank:</b> {{ seller.bank.bank_name }}<br>{% endif %}
    {% if seller.bank.account %}<b>A/C No.:</b> {{ seller.bank.account }}　{% endif %}
    {% if seller.bank.swift %}<b>SWIFT:</b> {{ seller.bank.swift }}{% endif %}
  </div>{% endif %}
{% endblock %}
```

```html
{# worker/templates/pi.html —— v1 与 quote 同结构（标题/英文名不同，由 worker 传 doc_en/title 区分）#}
{% extends "quote.html" %}
```

- [ ] **Step 4: 提交**

```bash
cd ~/chat2go && git add worker/templates/
git commit -m "feat(doc): 品牌模板 brand.css + base/quote/pi(绿色确认稿)"
```

---

### Task 4: `render_document` —— Jinja2 + WeasyPrint 出 PDF（TDD）

**Files:**
- Modify: `worker/doc_render.py`
- Test: `worker/test_doc_render.py`

- [ ] **Step 1: 加失败测试**

```python
# 追加到 test_doc_render.py
from pypdf import PdfReader
import io

class TestRender(unittest.TestCase):
    def _data(self):
        return {"doc_type": "quote", "doc_no": "QT-9", "date": "2026-06-01",
                "buyer": {"name": "EURO STANDARD"},
                "items": [{"name": "Acrylic Box", "qty": 2000, "unit_price": 3.85}],
                "trade_term": "CNF Busan",
                "terms": {"付款方式": "30% 定金"}}

    def test_quote_pdf_nonempty_and_text(self):
        prof = {"name_cn": "佛山外艾斯进出口贸易有限公司", "logo_text": "W",
                "bank": {"swift": "BKCHCNBJ400"}}
        pdf = dr.render_document("quote", self._data(), prof)
        self.assertGreater(len(pdf), 2000)
        text = "".join(p.extract_text() or "" for p in PdfReader(io.BytesIO(pdf)).pages)
        self.assertIn("QT-9", text)
        self.assertIn("EURO STANDARD", text)
        self.assertIn("报价单", text)

    def test_pi_renders(self):
        pdf = dr.render_document("pi", self._data(), {"name_cn": "X"})
        self.assertGreater(len(pdf), 2000)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd ~/chat2go/worker && python3 -m unittest test_doc_render -v`
Expected: FAIL（`AttributeError: module 'doc_render' has no attribute 'render_document'`）

- [ ] **Step 3: 实现 render_document**

```python
# 追加到 worker/doc_render.py
import os
from jinja2 import Environment, FileSystemLoader, select_autoescape

_TEMPLATE_DIR = os.path.join(os.path.dirname(os.path.abspath(__file__)), "templates")
_DOC_EN = {"quote": "Quotation", "pi": "Proforma Invoice"}

_env = Environment(
    loader=FileSystemLoader(_TEMPLATE_DIR),
    autoescape=select_autoescape(["html"]),
)


def render_document(doc_type: str, data: dict, profile: dict,
                    seal_png: bytes | None = None) -> bytes:
    """结构化数据 → PDF bytes。doc_type ∈ {quote, pi}。"""
    from weasyprint import HTML
    import base64

    ctx = prepare_context(doc_type, data, profile)
    ctx["doc_en"] = _DOC_EN.get(doc_type, "")
    ctx["seller_label"] = "报价方 / From Seller" if doc_type == "quote" else "卖方 / From Seller"
    ctx["seller_seal_label"] = "报价方盖章：" if doc_type == "quote" else "卖方盖章："
    if seal_png and data.get("stamp"):
        ctx["seal_img"] = "data:image/png;base64," + base64.b64encode(seal_png).decode()

    template = _env.get_template(f"{doc_type}.html")
    html_str = template.render(**ctx)
    return HTML(string=html_str, base_url=_TEMPLATE_DIR).write_pdf()
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd ~/chat2go/worker && python3 -m unittest test_doc_render -v`
Expected: PASS（含 TestPrepareContext 共 7 tests）

- [ ] **Step 5: 人眼视觉验证（关卡）**

```bash
cd ~/chat2go/worker && ~/.venv-c2g/bin/python -c "
import doc_render as dr, json
data={'doc_type':'quote','doc_no':'QT-9','date':'2026-06-01','buyer':{'name':'EURO STANDARD','address':'Seoul, Korea'},'items':[{'name':'Acrylic Box','spec':'320x240mm','qty':2000,'unit_price':3.85},{'name':'Magnet','qty':5000,'unit_price':1.2}],'extra_charges':[{'label':'Freight','amount':680}],'trade_term':'CNF Busan','terms':{'付款方式':'30% 定金，余款见提单','交货期':'30 天','原产地':'中国(可出 Form K)'}}
prof={'name_cn':'佛山外艾斯进出口贸易有限公司','name_en':'FOSHAN WAI-AISI IMP&EXP CO.,LTD.','logo_text':'W','contact':'李锐','tel':'+86-757-8888-0001','bank':{'beneficiary':'FOSHAN WAI-AISI','bank_name':'Bank of China','account':'7501xxxx392','swift':'BKCHCNBJ400'}}
open('/tmp/sample-quote.pdf','wb').write(dr.render_document('quote',data,prof))
print('wrote /tmp/sample-quote.pdf')
"
open -a "Google Chrome" /tmp/sample-quote.pdf
```
Expected: Chrome 打开样例 PDF，观感与确认稿一致（绿色页眉、货物表、合计、条款、银行、签章、页脚页码）。**停下来给用户看，确认观感。**

- [ ] **Step 6: 提交**

```bash
cd ~/chat2go && git add worker/doc_render.py worker/test_doc_render.py
git commit -m "feat(doc): render_document Jinja2+WeasyPrint 出 PDF"
```

---

### Task 5: 公章叠加（TDD）

**Files:**
- Test: `worker/test_doc_render.py`
- （实现已在 Task 4 的 render_document 内含 seal_img 分支，本任务补测试 + 校验 base.html 锚点）

- [ ] **Step 1: 加测试**

```python
# 追加到 test_doc_render.py
class TestSeal(unittest.TestCase):
    def _png(self):
        from PIL import Image
        import io
        b = io.BytesIO(); Image.new("RGBA", (200, 200), (200, 0, 0, 255)).save(b, "PNG")
        return b.getvalue()

    def _data(self):
        return {"doc_type": "quote", "buyer": {"name": "B"},
                "items": [{"name": "A", "qty": 1, "unit_price": 1}], "stamp": True}

    def test_stamp_embeds_image(self):
        with_seal = dr.render_document("quote", self._data(), {"name_cn": "X"}, seal_png=self._png())
        d2 = dict(self._data()); d2["stamp"] = False
        without = dr.render_document("quote", d2, {"name_cn": "X"}, seal_png=self._png())
        self.assertGreater(len(with_seal), len(without) + 100)
```

- [ ] **Step 2: 跑测试**

Run: `cd ~/chat2go/worker && python3 -m unittest test_doc_render -v`
Expected: PASS（render_document 已含 seal 分支；若失败检查 base.html 是否有 `{% if seal_img %}<img class="seal">`）

- [ ] **Step 3: 提交**

```bash
cd ~/chat2go && git add worker/test_doc_render.py
git commit -m "test(doc): 公章叠加单测"
```

---

### Task 6: 公司档案载入 `load_company_profile`（TDD 纯解析）

**Files:**
- Modify: `worker/trade_memory.py`
- Test: `worker/test_trade_memory.py`

- [ ] **Step 1: 加失败测试**

```python
# 追加到 test_trade_memory.py
class TestCompanyProfile(unittest.TestCase):
    def test_parse_valid_json(self):
        p = tm.parse_company_profile('{"name_cn":"佛山外艾斯","bank":{"swift":"X"}}')
        self.assertEqual(p["name_cn"], "佛山外艾斯")
        self.assertEqual(p["bank"]["swift"], "X")

    def test_parse_invalid_returns_empty(self):
        self.assertEqual(tm.parse_company_profile("not json"), {})
        self.assertEqual(tm.parse_company_profile(None), {})
        self.assertEqual(tm.parse_company_profile(""), {})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd ~/chat2go/worker && python3 -m unittest test_trade_memory -v`
Expected: FAIL（`AttributeError: ... parse_company_profile`）

- [ ] **Step 3: 实现**

```python
# 追加到 worker/trade_memory.py
import json

COMPANY_PROFILE_TITLE = "公司档案"

def parse_company_profile(content) -> dict:
    """记忆里的公司档案 content(JSON 字符串) → dict；解析失败/空 → {}。"""
    if not content or not isinstance(content, str):
        return {}
    try:
        obj = json.loads(content)
        return obj if isinstance(obj, dict) else {}
    except (ValueError, TypeError):
        return {}

def load_company_profile(sb, expert_id: str, product: str) -> dict:
    """从 tradego_memory_rules(kind='company', title=公司档案) 载入结构化公司档案。"""
    try:
        res = (sb.table("tradego_memory_rules")
               .select("content")
               .eq("expert_id", expert_id).eq("product", product)
               .eq("kind", "company").eq("title", COMPANY_PROFILE_TITLE)
               .limit(1).execute())
        rows = res.data or []
        return parse_company_profile(rows[0]["content"]) if rows else {}
    except Exception:
        return {}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd ~/chat2go/worker && python3 -m unittest test_trade_memory -v`
Expected: PASS（原有 + 2 新 test）

- [ ] **Step 5: 提交**

```bash
cd ~/chat2go && git add worker/trade_memory.py worker/test_trade_memory.py
git commit -m "feat(doc): load_company_profile 从记忆载入公司档案(纯解析 TDD)"
```

---

### Task 7: `make_document` 工具 schema（TDD shape）

**Files:**
- Modify: `worker/doc_render.py`（加 `DOCUMENT_TOOL_SCHEMA`）
- Test: `worker/test_doc_render.py`

- [ ] **Step 1: 加测试**

```python
# 追加到 test_doc_render.py
class TestSchema(unittest.TestCase):
    def test_schema_shape(self):
        s = dr.DOCUMENT_TOOL_SCHEMA
        self.assertEqual(s["name"], "make_document")
        props = s["input_schema"]["properties"]
        self.assertIn("doc_type", props)
        self.assertEqual(props["doc_type"]["enum"], ["quote", "pi"])
        self.assertEqual(set(s["input_schema"]["required"]), {"doc_type", "buyer", "items"})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd ~/chat2go/worker && python3 -m unittest test_doc_render -v`
Expected: FAIL（`AttributeError: ... DOCUMENT_TOOL_SCHEMA`）

- [ ] **Step 3: 实现（加到 doc_render.py 顶部常量区）**

```python
# worker/doc_render.py 内
DOCUMENT_TOOL_SCHEMA = {
    "name": "make_document",
    "description": ("生成品牌级外贸单证 PDF(报价单 quote / 形式发票 pi)。"
                    "只需给 buyer+items+条款,卖方抬头/银行/logo/公章由系统按公司档案自动填充。"
                    "要盖章传 stamp:true。必须真的调用本工具,严禁编造下载链接。"),
    "input_schema": {
        "type": "object",
        "properties": {
            "doc_type": {"type": "string", "enum": ["quote", "pi"]},
            "title_cn": {"type": "string"},
            "doc_no": {"type": "string"},
            "date": {"type": "string", "description": "YYYY-MM-DD,省略则用当天"},
            "currency": {"type": "string"},
            "validity": {"type": "string"},
            "buyer": {"type": "object", "properties": {
                "name": {"type": "string"}, "attn": {"type": "string"},
                "address": {"type": "string"}, "tel": {"type": "string"}},
                "required": ["name"]},
            "items": {"type": "array", "items": {"type": "object", "properties": {
                "name": {"type": "string"}, "spec": {"type": "string"},
                "qty": {"type": "number"}, "unit_price": {"type": "number"},
                "amount": {"type": "number"}}, "required": ["name", "qty", "unit_price"]}},
            "extra_charges": {"type": "array", "items": {"type": "object", "properties": {
                "label": {"type": "string"}, "amount": {"type": "number"}}}},
            "trade_term": {"type": "string"},
            "terms": {"type": "object", "additionalProperties": {"type": "string"}},
            "stamp": {"type": "boolean"},
        },
        "required": ["doc_type", "buyer", "items"],
    },
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd ~/chat2go/worker && python3 -m unittest test_doc_render -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
cd ~/chat2go && git add worker/doc_render.py worker/test_doc_render.py
git commit -m "feat(doc): make_document 工具 schema"
```

---

### Task 8: worker 接线 —— 注册工具 + dispatch

**Files:**
- Modify: `worker/chat2go_worker.py`（`tools=` 列表 ~664；dispatch ~678；import）

- [ ] **Step 1: import doc_render**

在文件顶部 import 区（与 `import doc_gen as dg` 同处）加：
```python
import doc_render as dr
```

- [ ] **Step 2: 注册工具（仅 trade 房）**

定位 `tools = ta.TOOL_SCHEMAS + dg.DOC_TOOL_SCHEMAS + tm.ORDER_TOOL_SCHEMAS + [tm.REMEMBER_TOOL_SCHEMA]`（~664），改为：
```python
    tools = ta.TOOL_SCHEMAS + dg.DOC_TOOL_SCHEMAS + tm.ORDER_TOOL_SCHEMAS + [tm.REMEMBER_TOOL_SCHEMA]
    if is_trade:
        tools = tools + [dr.DOCUMENT_TOOL_SCHEMA]
```
（`is_trade` 为当前函数内已有的 trade 房判断布尔；若变量名不同，沿用本函数已用于注入 memory/orders 的同一判断。）

- [ ] **Step 3: dispatch make_document**

定位工具分发处（`if tu.name in dg.DOC_BUILDERS:` ~678 之前）加一个分支：
```python
            if tu.name == "make_document":
                profile = tm.load_company_profile(sb, expert_id, product)
                seal_url = _pick_seal_url(img_choices) if (tu.input or {}).get("stamp") else None
                seal_png = None
                if seal_url:
                    raw = dg._fetch_bytes(seal_url)
                    seal_png = dg._seal_png(raw) if raw else None
                tin = dict(tu.input or {})
                tin.setdefault("date", tm._now_iso()[:10])
                pdf_bytes = dr.render_document(tin["doc_type"], tin, profile, seal_png)
                fname = f"{tin.get('doc_no') or tin['doc_type']}.pdf"
                att, out = _upload_doc_bytes(sb, pdf_bytes, fname, "application/pdf")
                # 复用现有 make_pdf 上传后的 attachment/结果处理路径
```
> **实现注意**：`_make_doc_and_upload`（~612）现有把 bytes 上传 Storage + 拼 attachment 的逻辑。抽出其中"bytes→上传→attachment"为 `_upload_doc_bytes(sb, data, filename, mime) -> (attachment, tool_result)`，make_pdf 和 make_document 共用，避免复制。`_pick_seal_url` / `dg._fetch_bytes` / `dg._seal_png` 均为现有函数。`expert_id` / `product` / `img_choices` 为本函数现有变量（与注入记忆/公章同源）。

- [ ] **Step 4: 本地冒烟（import 不报错 + 工具列表含 make_document）**

Run:
```bash
cd ~/chat2go/worker && ~/.venv-c2g/bin/python -c "import doc_render as dr; print(dr.DOCUMENT_TOOL_SCHEMA['name'])"
```
Expected: 打印 `make_document`

- [ ] **Step 5: 提交**

```bash
cd ~/chat2go && git add worker/chat2go_worker.py
git commit -m "feat(doc): worker 注册 make_document + dispatch(载档案+公章+渲染上传)"
```

---

### Task 9: Modal 镜像 —— WeasyPrint + 中文字体 + 模板目录

**Files:**
- Modify: `worker/chat2go_worker.py`（镜像定义 ~52-68）

- [ ] **Step 1: 改镜像定义**

在 `modal.Image.debian_slim(...)` 链上加（WeasyPrint 系统依赖 + 字体 + 模板目录 + pip）：
```python
    modal.Image.debian_slim(python_version="3.11")
    .apt_install(
        "libpango-1.0-0", "libpangocairo-1.0-0", "libgdk-pixbuf-2.0-0",
        "libffi-dev", "libcairo2", "fonts-noto-cjk", "fontconfig",
    )
    .pip_install(
        # …现有依赖… 末尾加：
        "weasyprint", "jinja2",
    )
    .run_commands("fc-cache -f")
    .add_local_python_source("trade_accounting")
    .add_local_python_source("doc_gen")
    .add_local_python_source("trade_memory")
    .add_local_python_source("doc_render")
    .add_local_dir("templates", "/root/templates")
```
> `fonts-noto-cjk`（apt 包，含 Noto Sans CJK SC）省去手动下载；brand.css 里 `font-family:"Noto Sans SC"` 能命中。`add_local_dir("templates", "/root/templates")` 把模板目录挂进镜像；**确认 worker 工作目录 = /root**（debian_slim 默认 /root），故 `doc_render._TEMPLATE_DIR`（= doc_render.py 同级 templates/）在镜像内需指向 `/root/templates`。

- [ ] **Step 2: 让 _TEMPLATE_DIR 兼容镜像路径**

`doc_render.py` 的 `_TEMPLATE_DIR` 改为优先环境变量、回退同级目录：
```python
_TEMPLATE_DIR = os.environ.get(
    "DOC_TEMPLATE_DIR",
    os.path.join(os.path.dirname(os.path.abspath(__file__)), "templates"),
)
```
并在镜像定义里设 `.env({"DOC_TEMPLATE_DIR": "/root/templates"})`（加在链上）。本地测试不设该 env，回退到 worker/templates（Task 3 路径），单测照常通过。

- [ ] **Step 3: 本地回归（确保改了路径后单测仍过）**

Run: `cd ~/chat2go/worker && python3 -m unittest test_doc_render -v`
Expected: PASS（全部）

- [ ] **Step 4: 提交**

```bash
cd ~/chat2go && git add worker/chat2go_worker.py worker/doc_render.py
git commit -m "build(worker): 镜像加 WeasyPrint+Noto CJK+模板目录"
```

---

### Task 10: 指引更新 TRADE_ACCOUNTING_GUIDE

**Files:**
- Modify: `worker/chat2go_worker.py`（TRADE_ACCOUNTING_GUIDE，~188 处文档工具段）

- [ ] **Step 1: 在文档工具说明里加 make_document 优先**

在现有"你配有 make_pdf/make_excel"段落后插入：
```
- **标准外贸单证(报价单 / 形式发票 PI)→ 必须用 `make_document`**(品牌级排版,样式统一)。
  只需给 doc_type(quote/pi) + buyer(客户) + items(货物:name/spec/qty/unit_price) +
  trade_term + terms(条款键值对);要盖章传 stamp:true。
  卖方公司抬头/银行/logo/公章**不用你填**,系统按公司档案自动套。
- 非标准/自由格式文档才用 make_pdf。
- 若公司档案还没建立,先用 remember 工具记一条 kind='company'、title='公司档案'、
  content 为 JSON(name_cn/name_en/address/tel/email/contact/logo_text/bank{...}),再出单证。
- 仍然:严禁编造下载链接,必须真的调用工具生成。
```

- [ ] **Step 2: 提交**

```bash
cd ~/chat2go && git add worker/chat2go_worker.py
git commit -m "docs(worker): 指引加 make_document 优先 + 公司档案建立指引"
```

---

### Task 11: 部署 + E2E 冒烟（关卡）

**Files:** 无（部署验证）

- [ ] **Step 1: 部署 worker**

Run: `cd ~/chat2go/worker && ~/.venv-c2g/bin/modal deploy chat2go_worker.py 2>&1 | tail -20`
Expected: 镜像构建成功(含 weasyprint/字体)，`✓ Created ... chat2go-worker`。构建失败先看 apt/字体日志。

- [ ] **Step 2: 在 iamarobot 测试房建公司档案 + 出报价单**

在 **iamarobot 房 `38ebcd0e`**（绝不用 live 小白房 `0ac15b5b`）发消息：
1. 先让 AI 记公司档案：「记一下我司：佛山外艾斯进出口贸易有限公司，英文 FOSHAN WAI-AISI IMP&EXP CO.,LTD.，银行 Bank of China，SWIFT BKCHCNBJ400，联系人李锐」
2. 再让 AI 出报价单：「给 EURO STANDARD 报个价：亚克力盒 2000 个 3.85 美元、磁吸 5000 个 1.2 美元，CNF 釜山，30% 定金，盖章」

- [ ] **Step 3: 看 log + 取 PDF 在真实 Chrome 验**

Run: `~/.venv-c2g/bin/modal app logs chat2go-worker 2>&1 | tail -20`
Expected: `room=38ebcd0e ... 200 OK`，无 WeasyPrint 异常。

从聊天里拿到生成的 PDF 链接，`open -a "Google Chrome" <url>` 打开 → 核对：绿色品牌报价单、外艾斯抬头、货物表/合计、条款、银行、**公章压在签字行**、页脚页码。**停下给用户确认观感。**

- [ ] **Step 4: 收尾**

```bash
cd ~/chat2go && git log --oneline -12   # 确认全部任务提交
```
最终代码评审 + finishing-a-development-branch。

---

## Self-Review

**Spec coverage**：渲染引擎(Task1,4,9)、make_document+只填数据(Task7,8)、品牌系统brand.css(Task3)、公司档案来自记忆(Task6,8)、公章HTML叠加(Task4,5)、中文字体(Task9)、make_pdf兜底保留(Task8仅加分支不删)、错误处理(prepare空档案不崩 Task2;parse失败→{} Task6)、测试(Task2/4/5/6/7单测+Task4/11视觉)、Phase1=quote+pi(全程)、隐私(模板/测试用占位,真实数据走DB Task6) —— 均有对应任务。
**Placeholder scan**：每个改代码步骤均给完整代码;Task8 dispatch 标注复用现有 `_upload_doc_bytes`(需从 `_make_doc_and_upload` 抽出)、`_pick_seal_url`/`dg._fetch_bytes`/`dg._seal_png`/`expert_id`/`product`/`img_choices` 均为现有符号。
**Type consistency**：`prepare_context(doc_type,data,profile)`→`render_document(doc_type,data,profile,seal_png)`、`parse_company_profile`/`load_company_profile`、`DOCUMENT_TOOL_SCHEMA`、ctx 键(seller/items/subtotal/total/seal_img)全程一致。
